services:
  app_spmi:
    build:
      context: .
      dockerfile: Dockerfile
    image: spmi_dev
    container_name: spmi_app
    restart: unless-stopped
    pull_policy: never
    ports:
      - "8187:80"
    volumes:
      - ./:/var/www/html
    depends_on:
      - db_spmi
    networks:
      - spmi-network

  db_spmi:
    image: mariadb
    container_name: spmi_db
    restart: always
    environment:
      MARIADB_ROOT_PASSWORD: admin123
      MARIADB_DATABASE: spmi
    ports:
      - "3306:3306"
    volumes:
      - mysql-data:/var/lib/mariadb
      - ./spmi.sql:/docker-entrypoint-initdb.d/spmi.sql
    healthcheck:
      test: ["CMD", "healthcheck.sh", "--connect", "--innodb_initialized"]
      interval: 10s
      timeout: 5s
      retries: 5  
    networks:
      - spmi-network

volumes:
  mysql-data:

networks:
  spmi-network:
    driver: bridge
